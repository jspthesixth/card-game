import { ApplicationLayout } from "@/layout/application-layout";
import { HomePage } from "@/pages/home";

export const App = () => {
  return (
    <ApplicationLayout>
      <HomePage />
    </ApplicationLayout>
  );
};
